server:
  port: 8082
  error:
    include-message: Always

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pd-course
    username: postgres
    password: 1123581321
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true
  output:
    ansi:
      enabled: Always

logging:
  level:
    com.pd: TRACE
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate: INFO